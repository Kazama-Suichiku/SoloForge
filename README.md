# SoloForge

> 一人公司多 Agent 桌面应用 —— 用 AI Agent 团队协作，让个人拥有整个公司的执行力。

SoloForge 是一款基于 Electron 的桌面应用，为个人创业者/独立开发者打造了一个**完整的虚拟公司团队**。多个 AI Agent 各司其职（秘书、CEO、CTO、CFO、CHRO），通过真实的组织架构、任务委派、绩效管理和项目协作来帮助你高效运营。

## 核心特性

### 🤖 多 Agent 协作系统

- **5 个核心 C-Level Agent**：秘书（路由调度）、CEO（战略决策）、CTO（技术架构）、CFO（财务预算）、CHRO（人事管理）
- **动态招聘**：CxO 可提出招聘需求，经 CHRO 审批后自动创建新 Agent 并入职
- **Agent 间通信**：委派任务、进度汇报、群聊讨论，支持 `@` 提及特定成员
- **层级响应**：群聊中按组织架构顺序响应

### 💬 智能聊天系统

- 私聊 & 群聊，支持流式输出和 Markdown 渲染
- 图片附件与语音输入（macOS / Windows 原生 STT）
- 工具调用可视化卡片，友好展示结果
- 聊天历史持久化，上下文自动裁剪

### 🛠 94+ 内置工具

| 类别 | 工具数 | 说明 |
|------|--------|------|
| 文件操作 | 3 | 读取、写入、列出文件 |
| Shell | 1 | 执行终端命令 |
| Git | 12 | 完整 Git 工作流（提交、PR、合并、分支管理） |
| 网络 | 2 | 多引擎网络搜索、网页抓取 |
| HR 管理 | 16 | 组织架构、招聘审批、绩效评估、晋升/降级、停职/复职 |
| 运营管理 | 15 | OKR 目标、KPI、任务看板、运营仪表盘 |
| 项目管理 | 10 | 项目、里程碑、任务分配、状态报告 |
| 协作通信 | 16 | Agent 间消息、任务委派、开发计划审批、群聊 |
| 记忆系统 | 7 | 知识存储/检索/搜索，公司信息、用户档案、项目上下文 |
| 财务 | 2 | Token 用量统计、预算设置 |
| TODO | 4 | 待办事项管理 |
| 其他 | 6+ | 计算器、报告生成、历史加载 |

### 📊 运营管理

- **OKR 体系**：创建战略目标，关联 KPI 指标
- **任务看板**：创建、分配、认领、汇报进度
- **运营仪表盘**：实时统计目标达成率、任务完成率
- **活动日志**：全员操作记录追踪

### 📋 项目管理引擎

- 项目 → 里程碑 → 任务三级管理
- 自动同步委派任务状态
- 逾期/阻塞任务检测
- 定期站会提醒
- 进度报告自动推送

### 🧠 记忆系统

- 多类型记忆：用户偏好、公司事实、项目上下文、决策记录、经验教训
- LLM 驱动的知识提取与摘要
- 时间衰减机制，定期维护
- 对话时自动召回相关记忆

### 🔍 任务巡查系统

- 每 5 分钟自动扫描待办任务
- 主动催促负责人处理超时任务
- 冷却机制防止重复提醒
- 支持手动开关（侧边栏 Toggle）

### 💰 预算与 Token 追踪

- 全局和 Agent 维度的 Token 用量统计
- 可设置每日/总量预算限额
- 预算预警通知
- CFO 工具实时查询开销

## 技术架构

```
┌─────────────────────────────────────────────┐
│                  Renderer                    │
│  React 18 + Zustand 5 + Tailwind CSS 3      │
│  Vite 5 (HMR dev / 静态构建 prod)            │
├─────────────────────────────────────────────┤
│              Preload (contextBridge)          │
│  IPC 通道 · electronAPI · soloforge API      │
├─────────────────────────────────────────────┤
│                Main Process                  │
│  ┌─────────┐ ┌──────────┐ ┌──────────────┐  │
│  │  Chat   │ │  Tools   │ │    LLM       │  │
│  │ Manager │ │ Executor │ │  Providers   │  │
│  └────┬────┘ └────┬─────┘ └──────┬───────┘  │
│       │           │              │           │
│  ┌────┴────┐ ┌────┴─────┐ ┌─────┴────────┐  │
│  │ Agents  │ │ 94+ 工具  │ │ Duojie/DS/   │  │
│  │ 5+N个   │ │ 权限检查  │ │ Ollama/OAI   │  │
│  └─────────┘ └──────────┘ └──────────────┘  │
│                                              │
│  Memory · Operations · PM · Budget · Patrol  │
│  Collaboration · HR · Account · Sync · STT   │
└─────────────────────────────────────────────┘
```

### 目录结构

```
src/
├── main/                   # Electron 主进程
│   ├── main.js             # 应用入口、窗口管理、IPC 注册
│   ├── chat/               # 聊天系统（Agent 定义、ChatManager）
│   ├── tools/              # 工具系统（94+ 工具、权限、执行器）
│   ├── llm/                # LLM 多 Provider（Duojie/DeepSeek/Ollama/OpenAI）
│   ├── config/             # Agent 配置、权限配置
│   ├── memory/             # 记忆系统（存储、检索、维护）
│   ├── operations/         # 运营数据（OKR/KPI/任务）
│   ├── pm/                 # 项目管理引擎
│   ├── budget/             # Token 追踪、预算管理、预警
│   ├── patrol/             # 任务巡查系统
│   ├── collaboration/      # Agent 间通信
│   ├── agent-factory/      # 动态 Agent 创建
│   ├── account/            # 多账号/多公司
│   ├── sync/               # Supabase 云同步
│   ├── stt/                # 语音识别（macOS/Windows）
│   └── attachments/        # 图片附件处理
├── preload/
│   └── preload.js          # contextBridge 安全桥接
├── renderer/               # React 前端
│   ├── App.jsx             # 应用根组件
│   ├── components/chat/    # 聊天 UI 组件
│   ├── pages/              # 设置、仪表盘、登录等页面
│   ├── store/              # Zustand 状态管理
│   └── hooks/              # 自定义 Hooks
└── shared/
    └── ipc-channels.js     # IPC 通道常量定义
```

### LLM Provider 支持

| Provider | 说明 | 环境变量 |
|----------|------|----------|
| **Duojie** | 多模型中转（Claude/Gemini/GPT/GLM） | `DUOJIE_API_KEY`, `DUOJIE_MODEL` |
| **DeepSeek** | deepseek-chat / deepseek-reasoner | `DEEPSEEK_API_KEY` |
| **Ollama** | 本地模型（localhost:11434） | — |
| **OpenAI** | GPT 系列 | `OPENAI_API_KEY`, `OPENAI_API_BASE_URL` |

支持自动降级：按优先级依次尝试，网络/限流错误自动重试。

## 快速开始

### 环境要求

- Node.js >= 18
- npm >= 9

### 安装

```bash
git clone git@github.com:Kazama-Suichiku/SoloForge.git
cd SoloForge
npm install
```

### 配置

在项目根目录创建 `.env` 文件：

```env
# 至少配置一个 LLM Provider
DUOJIE_API_KEY=your_api_key
DUOJIE_MODEL=claude-sonnet-4-5

# 可选
DEEPSEEK_API_KEY=your_key
OPENAI_API_KEY=your_key
OPENAI_API_BASE_URL=https://api.openai.com/v1
```

### 运行

```bash
# 开发模式（Vite HMR + Electron）
npm run dev

# 生产模式（构建后直接运行）
npm start
```

### 构建安装包

```bash
# macOS
npm run build:mac

# Windows
npm run build:win
```

## 依赖

### 运行时

- `electron` 33 — 桌面框架
- `react` 18 + `react-dom` 18 — UI 框架
- `zustand` 5 — 状态管理
- `react-markdown` + `remark-gfm` — Markdown 渲染
- `@heroicons/react` — 图标库
- `dotenv` — 环境变量
- `electron-store` — 本地持久化
- `idb` — IndexedDB 封装
- `@supabase/supabase-js` — 云同步（可选）

### 开发

- `vite` 5 + `@vitejs/plugin-react` — 前端构建
- `tailwindcss` 3 + `autoprefixer` + `postcss` — 样式
- `electron-builder` 24 — 打包
- `concurrently` + `wait-on` — 开发流程编排

## 许可证

私有项目，保留所有权利。
